<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhu.dao.TtypeDao">
    <select id="list" resultType="com.xhu.entity.Ttype">
        SELECT *
        FROM ttype
    </select>

    <insert id="add" parameterType="com.xhu.entity.Ttype">
        INSERT INTO ttype(name)
        VALUES (#{name})
    </insert>

    <delete id="delete">
        DELETE FROM ttype
        WHERE id = #{id}
    </delete>
</mapper>